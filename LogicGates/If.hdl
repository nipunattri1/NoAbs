CHIP If {
    IN cond, a, b;
    OUT out;

    PARTS:
    // if cond then => a else b
    Not(in=cond, out=notcond);
    And(a=a, b=cond, out=aAndcond); // returns a if condition is true else false
    And(a=b, b=notcond, out=bAndnotcond);
    Or(a=aAndcond, b=bAndnotcond, out=out);
}